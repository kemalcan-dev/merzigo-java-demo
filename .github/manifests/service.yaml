apiVersion: v1
kind: Service
metadata:
  name: merzigo-deploy
  namespace: java-demoapp-namespace
  labels:
    app.kubernetes.io/name: merzigo-deploy
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: merzigo-deploy
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080